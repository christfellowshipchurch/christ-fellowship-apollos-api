import {
    PrayerRequest as CorePrayerRequest
} from '@apollosproject/data-connector-rock';

export default class PrayerRequest extends CorePrayerRequest.dataSource {
    // customize or override data source methods
}